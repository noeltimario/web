import { initializeApp } from "firebase/app";
import { getAuth, GoogleAuthProvider } from "firebase/auth";

// Replace these values with your actual Firebase project config
const firebaseConfig = {
  apiKey: "AIzaSy...", 
  authDomain: "uc-smarthelp-xxxx.firebaseapp.com",
  projectId: "uc-smarthelp-xxxx",
  storageBucket: "uc-smarthelp-xxxx.appspot.com",
  messagingSenderId: "1234567890",
  appId: "1:1234567890:web:abcdef123456"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

// Export for use in Login.tsx
export const auth = getAuth(app);
export const googleProvider = new GoogleAuthProvider();

// Optional: Forces the Google account selection popup every time
googleProvider.setCustomParameters({
  prompt: 'select_account'
});